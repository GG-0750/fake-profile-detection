# Fake Profile Detection System

**Note:** All the current work is in the `dev` branch.  
The `main` branch is kept stable/empty.  
Please switch to `dev` to see the latest code:

```bash
git fetch origin
git checkout dev
git pull origin dev
